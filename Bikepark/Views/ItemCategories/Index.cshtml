@model IEnumerable<Bikepark.Models.ItemCategory>

@{
    ViewData["Title"] = "Item Categories";
    ViewData["menu"] = "storage";
    Layout = "_MainLayout";
}

<h3>Item Categories</h3>
<div class="d-flex flex-wrap justify-content-between align-items-center border-bottom">
    <ul class="nav col-md-6 justify-content-start">
        <li class="nav-item">
            <a class="nav-link" asp-action="Create">Add</a>
        </li> 
        <li class="nav-item">
            <a class="nav-link" asp-action="Index" asp-controller="Storage" >Storage</a>
        </li>     
    </ul>

    <ul class="nav  col-md-6 justify-content-end">
        <li class="nav-item">
            <a class="nav-link" asp-action="Export" style="display: none">Download the XLS</a>
        </li>    
        <li class="nav-item"> 
            <a class="nav-link" asp-action="Main" asp-controller="Storage">Management</a> 
        </li>    
    </ul>  
</div>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ItemCategoryName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Accessories)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ItemCategoryName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Accessories)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ItemCategoryID">Change</a> |
                <a asp-action="Delete" asp-route-id="@item.ItemCategoryID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
